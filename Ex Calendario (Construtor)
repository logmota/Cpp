/******************************************************************************

 Dado o exercício do calendário transforme-o em uma classe com seus 3
    métodos e crie um objeto no “main “ e manipule a classe.

                   
    Criar a classe com os atributos necessários, construtor, e esses 3 métodos
    na classe. No programa principal faça o objeto e imprima o calendário.



*******************************************************************************/

#include <iostream>
using namespace std;

class Calendario {
private:
    int mes;
    int ano;

public:
        Calendario(int me, int an) {
        mes = me;
        ano = an;
    }



//1º Método seria o ano bissexto 
int anoB(){

    bool anoB() {
        if (((ano % 4 == 0) && (ano % 100 != 0)) || (ano % 400 == 0))
            return true;
        else
            return false;
    }


//2º Método seria Dia da semana, esse método retorna um inteiro de 1 a 7 onde 
//seria os dias da semana domingo, segunda, terça e etc…


    int diaSem(int dia) {
                int a = ano;
        int m = mes;
        
        int f = a + dia + 3 * (m - 1) - 1;

        if (m < 3) a--;
        else f -= int(0.4 * m + 2.3);

        f += int(a / 4) - int((a / 100 + 1) * 0.75);
        f %= 7;

        return f + 1;
    }

//3º Método seria Imprimi Calendário, esse método seria prints para imprimir 
//o calendário referente a mês e ano. Tipo ele imprimi o mes de Maio do ano de
//2020, que dia da semana caiu o dia primeiro e imprimi até o último.

    void imprimirCalendario() {
        cout << "Calendario: " << mes << "/" << ano << endl;
        cout << "DOM\tSEG\tTER\tQUA\tQUI\tSEX\tSAB\n";

        short TamanhoDoMes[12] = {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};

        if (anoB()) {
            TamanhoDoMes[1] = 29;
        }

        
        int inicio = diaSem(1);
        for (int j = 1; j < inicio; j++) {
            cout << '\t';
        }

        for (int dia = 1; dia <= TamanhoDoMes[mes - 1]; dia++) {
            if (dia < 10) cout << '0' << dia << '\t';
            else cout << dia << '\t';

            
            if (diaSem(dia) == 7) cout << '\n';
        }
        cout << endl;
    }
};

int main() {
    
    Calendario meuCalendario(5, 2026);
    meuCalendario.imprimirCalendario();

    return 0;
}
